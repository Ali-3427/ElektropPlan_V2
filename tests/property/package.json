{
  "name": "@elektroplan/property-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b --pretty false",
    "test": "tsc -b && vitest run --configLoader native"
  },
  "dependencies": {
    "@elektroplan/calculation-core": "workspace:*",
    "@elektroplan/calculation-data": "workspace:*",
    "fast-check": "^3.22.0",
    "vitest": "^3.1.2"
  }
}
